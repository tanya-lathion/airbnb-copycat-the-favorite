<div id="navbarSupportedContent">
  <div class="navbar-nav me-auto ">

    <div id="navbarDropdown" class="fs-4 dropdown-menu-btn bg-white rounded-pill px-3 py-0 shadow-sm" data-bs-toggle="dropdown", aria-haspopup="true" aria-expanded="false">
      <i class="ri-menu-line"></i>
      <% if current_user %>
        <i class="ri-user-smile-line hover-effect"></i>
      <% else %>
        <i class="ri-user-line hover-effect"></i>
      <% end %>
    </div>
    <div class="user-select-none dropdown-menu dropdown-menu-end p-3 shadow-sm" aria-labelledby="navbarDropdown">

      <% if user_signed_in? %>
        <%= link_to "Dashboard", dashboard_path, class: "dropdown-item rounded-pill" %>
        <%= link_to "Log out", destroy_user_session_path, class: "dropdown-item rounded-pill", data: {turbo_method: :delete} %>
      <% else %>
        <%= link_to "Sign Up", new_user_registration_path, class: "dropdown-item rounded-pill" %>
        <%= link_to "Login", new_user_session_path, class: "dropdown-item rounded-pill" %>
      <% end %>




    </div>
  </div>
</div>
